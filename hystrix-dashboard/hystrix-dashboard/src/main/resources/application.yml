server:
  port: 8099

spring:
  application:
    name: HYSTRIX-DASHBOARD
  cloud:
    config:
      enabled: true
      uri: http://localhost:8098

hystrix:
  dashboard:
    proxy-stream-allow-list: "*"